{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "dynamic-as",
  "type": "registry:lib",
  "title": "Dynamic As",
  "description": "DynamicAsProps helper for polymorphic Zag parts",
  "dependencies": [
    "solid-js"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "registry/warsaw/lib/dynamic-as.ts",
      "content": "import type { ComponentProps, ValidComponent } from \"solid-js\"\n\n/**\n * Polymorphic part props: `as` + Zag part props.\n * Kept loose so Dynamic + Zag prop objects type-check cleanly.\n */\nexport type DynamicAsProps<\n  C extends ValidComponent,\n  P,\n  CP = ComponentProps<C>,\n> = {\n  [K in keyof P | keyof CP]: K extends keyof P\n    ? P[K]\n    : K extends keyof CP\n      ? CP[K]\n      : never\n} & {\n  as?: C | undefined\n}\n",
      "type": "registry:lib",
      "target": "registry/warsaw/lib/dynamic-as.ts"
    }
  ]
}