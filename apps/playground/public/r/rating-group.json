{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "rating-group",
  "type": "registry:ui",
  "title": "Rating Group",
  "description": "Zag compound createRatingGroup() — https://zagjs.com/components/solid/rating-group",
  "dependencies": [
    "@zag-js/rating-group",
    "@zag-js/solid",
    "solid-js"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "registry/warsaw/ui/rating-group.tsx",
      "content": "import * as zag from \"@zag-js/rating-group\"\nimport { mergeProps, normalizeProps, useMachine } from \"@zag-js/solid\"\nimport {\n  Show,\n  createMemo,\n  createUniqueId,\n  splitProps,\n  type JSX,\n  type Component,\n} from \"solid-js\"\nimport { Dynamic } from \"solid-js/web\"\n\ntype PartProps = {\n  as?: Component<Record<string, unknown>> | keyof JSX.IntrinsicElements\n  children?: JSX.Element\n} & Record<string, unknown>\n\nexport type CreateRatingGroupOptions = Record<string, unknown>\n\n/**\n * Zag rating-group compound. Call inside a Solid component setup (uses useMachine).\n *\n * @see https://zagjs.com/components/solid/rating-group\n *\n * ```tsx\n * import { createRatingGroup } from \"@components/ui/rating-group\"\n *\n * const ratingGroup = createRatingGroup({ openDelay: 200 })\n * return (\n *   <ratingGroup.Root>\n *     ...\n *   </ratingGroup.Root>\n * )\n * ```\n */\nexport function createRatingGroup(options: CreateRatingGroupOptions = {}) {\n  const service = useMachine(zag.machine, {\n    id: createUniqueId(),\n    ...options,\n  })\n  const api = createMemo(() => zag.connect(service, normalizeProps))\n\n  return {\n    Root(props: PartProps) {\n      const [local, rest] = splitProps(props, [\"as\", \"children\"])\n      const getProps = api().getRootProps\n      return (\n        <Dynamic\n          component={local.as ?? \"div\"}\n          {...(getProps ? mergeProps(getProps(), rest) : rest)}\n        >\n          {local.children}\n        </Dynamic>\n      )\n    },\n\n    Label(props: PartProps) {\n      const [local, rest] = splitProps(props, [\"as\", \"children\"])\n      const getProps = api().getLabelProps as ((p?: Record<string, unknown>) => Record<string, unknown>) | undefined\n      return (\n        <Dynamic\n          component={local.as ?? \"label\"}\n          {...mergeProps(getProps ? getProps(rest) : { \"data-part\": \"label\" }, rest)}\n        >\n          {local.children}\n        </Dynamic>\n      )\n    },\n\n    Item(props: PartProps) {\n      const [local, rest] = splitProps(props, [\"as\", \"children\"])\n      const getProps = api().getItemProps as ((p?: Record<string, unknown>) => Record<string, unknown>) | undefined\n      return (\n        <Dynamic\n          component={local.as ?? \"div\"}\n          {...mergeProps(getProps ? getProps(rest) : { \"data-part\": \"item\" }, rest)}\n        >\n          {local.children}\n        </Dynamic>\n      )\n    },\n\n    Control(props: PartProps) {\n      const [local, rest] = splitProps(props, [\"as\", \"children\"])\n      const getProps = api().getControlProps as ((p?: Record<string, unknown>) => Record<string, unknown>) | undefined\n      return (\n        <Dynamic\n          component={local.as ?? \"div\"}\n          {...mergeProps(getProps ? getProps(rest) : { \"data-part\": \"control\" }, rest)}\n        >\n          {local.children}\n        </Dynamic>\n      )\n    },\n\n    /** Connected Zag API (accessor). */\n    api,\n  }\n}\n\nexport type RatingGroupCompound = ReturnType<typeof createRatingGroup>\n",
      "type": "registry:ui",
      "target": "components/ui/rating-group.tsx"
    }
  ]
}